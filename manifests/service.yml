apiVersion: v1
kind: Service
metadata:
  name: axion-ingestion-service
  namespace: axion-app
spec:
  type: ClusterIP
  selector:
    app: axion-ingestion-service
  ports:
    - port: 80
      targetPort: 8000